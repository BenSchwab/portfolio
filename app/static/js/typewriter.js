/*
This is a script that will given a file will type it out as if writting from a typewriter.
*/

function outputText(file, canvas){
   /*ctx = canvas[0].getContext("2d");
   ctx.fillStyle = '#000000';
   ctx.fillRect(0,0,500,500);
   ctx.fillStyle = '#FFFFFF';
   //alert(file);
   var text = file.substring(1,10);
   ctx.fillText(text,10,10);
   //alert("width"+canvas.width); */

}